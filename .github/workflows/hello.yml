name: deploy

on:
  push:
    branches:
      - master
jobs:
  build:
    name: Greeting
    # This job runs on Linux
    runs-on: ubuntu-latest
    steps:
      - name: working dir
        run: |
          pwd
          ls -la
      - name: My first step
        uses: ./.github/actions/hello
