name: hello
on: [push]
jobs:
  hello-date:
    runs-on: ubuntu-latest
    steps:
      - run: echo "MY_DATE=$(date +'%Y-%m-%d')" >> $GITHUB_ENV
      - run: echo ${{ env.MY_DATE }}
  directories:
    runs-on: ubuntu-latest
    steps:
      - run: echo ls -l
